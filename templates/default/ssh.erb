table filter {
  chain INPUT {
    <% Array(@port).each do |port| %>
      proto tcp dport <%= port %> ACCEPT;
    <% end %>
  }
}

domain ip6 table filter {
  chain INPUT {
    <% Array(@port).each do |port| %>
      proto tcp dport <%= port %> ACCEPT;
    <% end %>
  }
}

